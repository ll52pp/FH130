 2018-03-30 从沈阳航盛取回来的代码。 该代码中去掉了ELS函数体, 只保留了空函数。

该版本进行过代码走读，进行过如下修改：
1) 增加API函数中状态的判断，参数值的判断；
2) 对NC/BNC切换代码进行了补充， 如果NC闪断但没有超时，则不影响当前网络的状态；
3) 修改了ELS接收方式。 原来是用els key去从接收帧队列中中搜寻对应key收包， 改成从接收帧队列头部进行收报；
4) 修改了某些ELS帧的网络序处理。


 2018-04-13修改：
1) 增加了配置下发过程中对数据的检查；
2) 重新整理了错误码定义， 按照子功能分别进行了定义。
3) 增加FPGA无buffer可用时的主动丢弃功能；（该功能是后期需求更改，版本交接过程中遗失了，现在补回来）
4) 配置下发过程中，修改对发送优先级的判断；（该功能是调试问题修改，版本交接过程中遗失了，现在补回来）
5) 部分参数检查加入ASSERT。
6) hwGetFpgaVersion修改为获取整个FPGA版本寄存器。.
7) pHbaDevCB统一修改为dc，保持风格一致；
8) FC_MSG_SEND_ITEM的注释；
9) 立即数用宏表示；
10) spi.c中不用的代码分支删除；
11) print_w_ctrl等与驱动业务有关的调试代码一到dev.c文件中， utils.c只保留工具函数

－－－－－－－－－－－－－－－－－－－－－－－－－－－－－
后续的修改，请更新这个文件。